# ZenTasks – GNOME Shell Extension

A lightweight productivity extension for GNOME Shell.  
ZenTasks integrates:

- Jira task tracking
- Pomodoro timer
- Workload & estimation helper
- Top-bar indicator for daily tasks

## Installation (Development)

Clone the repo:

```bash
git clone https://github.com/<your-user>/gnome-shell-extension-zentasks
cd gnome-shell-extension-zentasks
